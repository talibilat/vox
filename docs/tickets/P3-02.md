# P3-02 Process Notes

Date: 2026-07-07.

## Worktree

Work was performed in the treehouse leased worktree `/Users/talibilat/.treehouse/vox-590e80/1/vox` (lease holder P3-02).
The branch is `p3-02-release`, developed stacked on `p3-01-tuning` (the release quotes its numbers) and rebased onto `origin/main` once PR #31 merged.

## What was done

- `LICENSE`: MIT, per the Phase 0 audit's recommendation (docs/licenses.md).
- `README.md`: full rewrite as the release front page: what Earshot is, install (`pip install earshot-cli`), a literal single-agent quickstart, the multi-agent fleet walkthrough, local vs API mode, the tuned-defaults story with the measured interrupt numbers, the Claude Code voice-mode positioning (validation, not competition, referencing anthropics/claude-code #34305 and #36745), the parking lot, and dev instructions.
- `docs/config-reference.md`: every knob in the shipped schema with defaults and meanings.
- `docs/demo-script.md`: the exact 90-second recording flow (wake, instruct, spoken response, barge-in, second agent by name, agent status, read-back, clean stop) so the recording session is just following the script.
- Packaging: release metadata in pyproject (authors, keywords, classifiers, project URLs), `uv build` producing the 0.1.0 sdist and wheel.
- Clean-machine verification: the wheel installed into a pristine venv with an empty HOME ran the quickstart literally: `earshot --version`, first-run config generation, `start`, `status`, `interrupt`, `stop`, all correct.
- GitHub housekeeping: repo description and topics set via the API.
- Release polish found during verification: a lone default agent no longer warns about its own name (spoken names only reach STT with two or more agents; single-agent routing is verbatim pass-through).

## Deliberately left for the human (flagged, not skipped silently)

- **Repo rename** `vox` to `earshot`: the issue itself says to flag before doing it, and a rename breaks every local clone's remote. One click in GitHub settings (or `gh repo rename earshot`) when ready; nothing in the code assumes the repo name.
- **PyPI publish**: no PyPI credentials exist on this machine. The artifacts are built and verified; publishing is `uv publish` (or `twine upload dist/*`) with a PyPI token. `earshot-cli` was verified available during planning.
- **Demo recording**: needs a human voice; the script makes it a 10-minute session. The README links the script and marks the recording as pending.

## Parking lot

Recorded as a README section (the issue's allowed alternative to filing issues): per-agent voices, finish chime, "read the last thing again."

## Verification that ran

- `pytest -q`: full suite green after the release changes.
- `ruff check` and `ruff format --check`: clean.
- The clean-venv wheel quickstart above.

## Documentation

This ticket added `LICENSE`, rewrote `README.md`, and added `docs/config-reference.md`, `docs/demo-script.md`, and `docs/tickets/P3-02.md`.
The README rewrite is this issue's core deliverable, so the shared-file caution was deliberately overridden with full ownership.
