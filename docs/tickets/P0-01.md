# P0-01 Process Notes

Date: 2026-07-06.

## Worktree

Work was performed in the treehouse leased worktree `/Users/talibilat/.treehouse/vox-590e80/1/vox`.
The branch is `p0-01-verify-licenses`.
The branch was created from `origin/main` after `git fetch origin`.
Before changes, `git rev-parse HEAD` and `git rev-parse origin/main` both returned `a25f5c17ab5f3dbf5ee23aa843309ae8bedb411d`.

## Inputs Checked

`README.md` existed but only contained the project title.
`voice-terminal-project-plan.md` and `voice-terminal-grilling-log.md` were not present on `origin/main`.
`docs/dependency-graph.md` was read and confirmed that issue #1 gates #5, #6, and #16.
`gh issue view 1 --repo talibilat/vox` was read and issue #1 was open.
`docs/licenses.md` did not exist on `origin/main` before this work.

## Scratch Work

Scratch clones were placed under `/private/tmp/earshot-p0-01-licenses`.
The cloned repositories and checked revisions are listed in `docs/licenses.md`.
The temporary VoiceMode MCP config was placed at `/private/tmp/earshot-p0-01-licenses/voicemode-mcp.json`.

## Environment Changes

The first Claude Code to VoiceMode MCP run reached VoiceMode but failed because `ffmpeg` was not installed.
`brew install ffmpeg` was run because VoiceMode's macOS setup docs list `ffmpeg` as a requirement.
After that, the same Claude Code MCP smoke test succeeded with TTS skipped and no microphone wait.

## Documentation

This ticket added `docs/licenses.md`.
This ticket added `docs/voicemode-notes.md`.
This ticket added `docs/tickets/P0-01.md`.
No existing shared Markdown file was edited.

## Revalidation on 2026-07-06

A prior session committed the findings above but ended before the no-mistakes gate ran, leaving the branch unvalidated.
A follow-up session independently re-verified every claim before validating.
All eleven scratch clones still existed at the exact revisions cited in `docs/licenses.md`, and each LICENSE file was re-read directly: the MIT, Apache-2.0, and MPL-2.0 findings all matched, the NTM OpenAI/Anthropic rider text was confirmed verbatim, and Tmux-Orchestrator was confirmed to have no license file (README-only MIT claim).
The openWakeWord CC BY-NC-SA 4.0 pretrained-model restriction, the Silero VAD noncommercial dataset note, and the Apache-licensed Kokoro weights claim were re-confirmed from the repository READMEs.
The VoiceMode-to-Claude-Code MCP path was re-run end to end with `claude -p --mcp-config ... --strict-mcp-config --allowedTools mcp__voicemode__converse` and the `converse` tool call succeeded (`skip_tts=true`, `wait_for_response=false`).
