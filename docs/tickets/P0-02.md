# P0-02 Process Notes

Date: 2026-07-07.

## Worktree

Work was performed in the treehouse leased worktree `/Users/talibilat/.treehouse/vox-590e80/1/vox` (lease holder P0-02).
The branch is `p0-02-control-plane-spike`, created from `origin/main` after `git fetch origin`.
Before changes, `git rev-parse HEAD` and `git rev-parse origin/main` both returned `a25f5c17ab5f3dbf5ee23aa843309ae8bedb411d`.

## Inputs checked

`gh issue view 2 --repo talibilat/vox` was read; issue #2 was open.
`docs/control-plane-spike.md` did not exist on `origin/main` before this work, and no `p0-02` branch existed.
The grilling log decision to reject raw SQLite reads was honored: everything goes through the HTTP/SSE API.

## What was done

The local opencode 1.17.14 install was confirmed to expose `serve` with `--port` and `--hostname` flags.
The API was discovered from the server's own OpenAPI document at `GET /doc` (162 paths) rather than external docs.
`spikes/opencode_serve_spike.py` was written as throwaway stdlib-only Python.
It spawns and owns the `opencode serve` child process, waits for readiness via `/api/health`, creates a session with an explicitly pinned model, runs a two-turn conversation with SSE streaming and event-based completion detection, and tears down cleanly.

Verification runs, all executed from the worktree:

- Single two-turn run: both responses printed, multi-turn persistence confirmed (turn two recalled turn one's marker without it being restated).
- `--loop 10`: 10 consecutive full-lifecycle runs passed 10/10 with no hangs and no truncated reads.
- `--kill-restart`: SIGKILL mid-turn, restart, session and messages survived, orphaned-prompt behavior recorded in the spike doc.

Findings, event payload samples, quirks, and the GO verdict are in `docs/control-plane-spike.md`.

## Documentation

This ticket added `docs/control-plane-spike.md`, `docs/tickets/P0-02.md`, and `spikes/opencode_serve_spike.py`.
No existing shared Markdown file was edited.
